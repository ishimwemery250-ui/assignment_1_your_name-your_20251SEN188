# assignment_1_your_name-your_20251SEN188
# Assignment 1 - SQL
**Name:** Ishimwe Emery 
**Student ID:** 20251SEN188 
**DBMS Used:** PostgreSQL 
## About this assignment For this assignment, I used PostgreSQL to create a small supermarket database. I created the tables, added data, and practiced JOINs, CTEs, and window functions. The database has customers, products, orders, and order items.
## Where to find my work The screenshots of my work are inside the `screenshots` folder. 
JOIN queries are in `screenshots/join queries`
 CTE query is in `screenshots/CTE query`
 Window function queries are in `screenshots/window_function_queries`
 The table creation screenshot is also inside the `screenshots` folder
 The inserted data for the tables is also inside the `screenshots` folder
 ## What I did I used JOINs to connect related tables and show customer, order, and product information. I used a CTE to calculate how much each customer spent and find customers who spent above the average. I also used window functions to:
          -  - rank customers by spending
          - - number customer orders
          -  - calculate running revenue
          -  - find the number of days between orders
## Challenge The part I found more difficult was understanding CTEs and window functions. I understood them better by running each query step by step and checking the result.
 ## How to run Open PostgreSQL or pgAdmin. Run the table creation first, then insert the data. After that, run the JOIN, CTE, and window function queries and check the results
